# GeoNames city lookup (https://www.geonames.org/login — free registration)
# Required for dynamic city detection on the globe.
# Without this, city detection is silently disabled and country-level features still work.
VITE_GEONAMES_USERNAME=anarchist123
